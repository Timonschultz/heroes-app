package nl.timonschultz.heroes.heroesapp.persistence.icon;

import org.springframework.data.jpa.repository.JpaRepository;

public interface HeroIconRepository extends JpaRepository<HeroIconEntity, Long> {
}
